# PROFILE UX Fix Plan

## 1. 推荐方案

**推荐方案 A，辅以方案 B 作为兜底。**

原因很直接：

- 上传后的资料已经写入 `Experience`，问题不在保存链路，而在“用户看见什么”。
- 当前 `/profile` 默认固定打开 `basic`，而上传解析出来的内容通常落在 `education`、`project`、`skills`。
- 所以最小改动应该先让用户**保存后立刻看到刚写入的模块**，而不是继续停在空的基础信息页。

推荐落地方式：

- 保存成功后跳转到 `/profile?focus=education`、`/profile?focus=project`、`/profile?focus=skills` 这类定位页
- `/profile` 默认不再死开 `basic`，而是优先打开“最近新增资料所在模块”

这样能同时解决：

- “我到底有没有保存成功”
- “资料去哪了”
- “为什么页面看起来还是空的”

## 2. 改动范围

只动前端体验，不动数据库和解析链路。

### 需要动的地方

- `UploadForm`
  - 保存成功后增加跳转
  - 携带模块定位参数
- `ProfilePage` / `ProfileForm`
  - 读取 `focus` 或类似参数
  - 初始化时打开对应模块
  - 如果没有参数，自动打开第一个非空模块

### 不需要动的地方

- 数据库结构
- Prisma schema
- 上传解析逻辑
- `Experience` 写入逻辑
- 个人资料字段设计

## 3. 用户体验变化

修改后，用户路径会变成：

上传并保存
→ 自动进入个人资料页
→ 直接看到“教育背景 / 项目经历 / 技能”等刚写入的模块
→ 页面顶部能明确感知到“资料已保存”

用户不会再产生“看起来什么都没变”的误解。

## 4. 风险评估

### 风险 1：模块定位不够准

上传解析结果可能同时包含多个模块，自动定位只打开其中一个。

**影响**：低。

**原因**：即使只定位一个模块，也比停留在 `basic` 好很多。

### 风险 2：用户从导航直接进 `/profile`

如果没有跳转参数，用户仍可能先看到 `basic`。

**缓解**：

- 默认改成“最近新增资料模块”
- 或者默认打开第一个有内容的模块

### 风险 3：新增“全部资料”视图会变重

这会让页面改动更大，也会增加信息密度。

**结论**：当前 MVP 不优先做。

## 5. 为什么比其他方案更适合当前 MVP

### 比方案 B 更好

方案 B 是“Profile 默认打开最近新增模块”。

它能缓解问题，但用户上传完成后，如果没有明显跳转动作，还是容易觉得“没有反馈”。

方案 A 直接告诉用户：

- 资料已经保存
- 现在去看的就是刚保存的内容

反馈更强，信任感更高。

### 比方案 C 更好

方案 C 的“全部资料”视图更完整，但属于产品形态升级，不是最小修复。

它会带来：

- 更复杂的信息架构
- 更多页面状态
- 更大的改造范围

对当前 MVP 来说，成本偏高。

### 结论

当前问题本质是“保存后反馈不清晰”，最合适的修复不是重做资料中心，而是：

1. 保存后直接把用户带回正确模块
2. Profile 默认打开最近新增内容

这是一刀见血、改动小、收益高的方案。
